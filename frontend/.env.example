# Copy to `.env.local` for local development (Next.js loads .env.local automatically).
# Base URL for the Overfished BFF (FastAPI). No trailing slash required.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
